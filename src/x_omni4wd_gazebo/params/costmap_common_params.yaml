# Common parameters for both global and local costmaps
obstacle_range: 3.0
raytrace_range: 3.5
footprint: [[-0.15, -0.15], [-0.15, 0.15], [0.15, 0.15], [0.15, -0.15]]
inflation_radius: 0.2

# Observation sources
observation_sources: scan

# Đồng bộ diffbot_navigation: expected_update_rate tránh "sensor out of date". Topic thường /scan hoặc scan.
scan: {sensor_frame: lidar_link, data_type: LaserScan, topic: scan, marking: true, clearing: true}


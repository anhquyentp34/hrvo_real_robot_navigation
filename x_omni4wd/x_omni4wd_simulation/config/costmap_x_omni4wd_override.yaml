# Override sensor_frame cho x_omni4wd (lidar_link, topic /scan) khi dùng costmap_common_params_velodyne.
obstacle_layer:
  observation_sources: laser
  laser:
    topic: /scan_filtered
    sensor_frame: lidar_link
    data_type: LaserScan
    marking: true
    clearing: true
    expected_update_rate: 0.5
    inf_is_valid: true
